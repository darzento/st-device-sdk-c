if(${CONFIG_STDK_IOT_CORE_SECURITY_BACKEND_HARDWARE_VIRTUAL})
	add_subdirectory(virtual)
elseif(${CONFIG_STDK_IOT_CORE_SECURITY_BACKEND_HARDWARE_ESE})
	add_subdirectory(se)
	target_sources(iotcore
		PRIVATE
		iot_security_be_hardware.c
	)
endif()
